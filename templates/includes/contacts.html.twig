<section class="bg-dark text-white"id="contact">
    <div class="container">
        <div class="row">
            <div class="col-lg-8 mx-auto text-center">
                <h2 class="section-heading">ok</h2>
                <hr class="my-4">
                <p class="mb-5">well</p>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-4 ml-auto text-center">
                <i class="fa fa-phone fa-3x mb-3 sr-contact"></i>
                <p>{{ get_setting('mobile_phone') }}</p>
            </div>
            <div class="col-lg-4 mr-auto text-center">
                <i class="fa fa-envelope-o fa-3x mb-3 sr-contact"></i>
                <p>
                    <a href="mailto:your-email@your-domain.com">{{ get_setting('email') }}</a>
                </p>
            </div>
            <div class="col-lg-4 mr-auto text-center">
                <i class="fa fa-facebook-f fa-3x mb-3 sr-contact"></i>
                <p>
                    <a href="https://www.facebook.com/{{ get_setting('facebook') }}">{{ get_setting('owner_name') }}</a>
                </p>
            </div>
        </div>
    </div>
</section>
