<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
        <img class="img-fluid" src="img/eoslogo.png" alt="">
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="{{ path('homepage') }}">Pagrindinis</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="veislynas.html">Veislynas</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="mususunys.html">Mūsų šunys</a>
                    <ul>
                        <div class ="row">
                            <div class = "column">
                                <h3>Patinai</h3>
                                {% for dog in dogs if dog.gang == 'Patinai' %}
                                    <a class="nav-link js-scroll-trigger" href="{{ path('suo', {slug: dog.slug}) }}">{{ dog.name }}</a>
                                {% endfor %}
                            </div>

                            <div class = "column">
                                <h3>Kalės</h3>
                                {% for dog in dogs if dog.gang == 'Kales' %}
                                    <a class="nav-link js-scroll-trigger" href="{{ path('suo', {slug: dog.slug}) }}">{{ dog.name }}</a>
                                {% endfor %}
                            </div>

                            <div class = "column">
                                <h3>Palikę mus</h3>
                                {% for dog in dogs if dog.gang == 'Palikę mus' %}
                                    <a class="nav-link js-scroll-trigger" href="{{ path('suo', {slug: dog.slug}) }}">{{ dog.name }}</a>
                                {% endfor %}
                            </div>
                        </div>
                    </ul>
                </li>
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="parodos.html">Parodos</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link js-scroll-trigger" href="kontaktai.html">Kontaktai</a>
                </li>
            </ul>
        </div>
    </div>
</nav>
